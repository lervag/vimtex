\documentclass{article}
\newcommand{\comm}{%
  % something here
  \textbf{Test}
  \ifx
    \tmp\atmp\relax
  \else
    \relax
  \fi
  \ifx\tmp\atmp\relax\else\relax\fi
  \ifx\tmp\atmp\relax\else\relax\fi
  \ifx\tmp\atmp\relax\else\relax\fi
  \ifx\tmp\atmp\relax\else\relax\fi
}

\begin{document}
\end{document}
