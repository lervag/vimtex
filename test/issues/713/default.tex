%! TeX program = lualatex
%! TeX program = pdflatex
%! TeX program = context (pdftex)
%! TeX program = context (luatex)
%! TeX program = context (xetex)
%! TeX program = XeLaTeX

\documentclass{minimal}
\begin{document}

Hello World!

\end{document}
