[tools]
hugo = "latest"
uv = "latest"
"github:orhun/git-cliff" = "2.12.0"

[tasks.web-build]
run = "web/scripts/build.sh"

[tasks.web-host]
depends = ["web-build"]
run = "python -m http.server -d web/public 8000"
